<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/values"
    tools:context=".SplashActivity">

    <!-- Logo (Starts from the top and moves to center) -->

    <!-- App Name (Starts from bottom and moves to center) -->
    <ImageView
        android:id="@+id/logo_splash"
        android:layout_width="192dp"
        android:layout_height="238dp"
        android:scaleType="fitCenter"
        android:src="@drawable/plane_logo"
        app:layout_constraintBottom_toTopOf="@id/Title_splash"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <TextView
        android:id="@+id/Title_splash"
        android:layout_width="237dp"
        android:layout_height="44dp"
        android:text="🦷 Pearl Dental App"
        android:textColor="@color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.606"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/logo_splash"
        app:layout_constraintVertical_chainStyle="packed" />

</androidx.constraintlayout.widget.ConstraintLayout>
